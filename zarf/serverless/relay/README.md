# Relay serverless configs

These files are legacy relay deployment artifacts kept only for compatibility work.
Relay deployment is unsupported in this release, and the packaged relay entrypoints now
fail closed with an `unsupported` message.
